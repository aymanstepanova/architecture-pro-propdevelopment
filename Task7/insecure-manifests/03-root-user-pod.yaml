apiVersion: v1
kind: Pod
metadata:
  name: pod-root-user
  namespace: audit-zone
spec:
  securityContext:
    runAsUser: 0
    runAsGroup: 0
  containers:
    - name: nginx
      image: nginx:stable
      command: ["sh", "-c", "id && nginx -g 'daemon off;'"]
