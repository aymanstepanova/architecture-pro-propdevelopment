---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: cluster-config-manager
  labels:
    app: propdevelopment
    role: cluster-config-manager
rules:
  - apiGroups: ["", "apps", "batch", "extensions", "networking.k8s.io"]
    resources:
      - pods
      - pods/log
      - deployments
      - statefulsets
      - daemonsets
      - jobs
      - cronjobs
      - services
      - ingresses
      - configmaps
      - secrets
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["get", "list", "watch"]